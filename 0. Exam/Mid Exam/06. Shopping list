my_list = input().split('!')
command = input()

while command != 'Go Shopping!':
    if 'Urgent' in command:
        task = command.split(' ')
        item = task[1]
        if item not in my_list:
            my_list.insert(0, item)

    elif 'Unnecessary' in command:
        task = command.split(' ')
        item = task[1]
        if item in my_list:
            my_list.remove(item)

    elif 'Correct' in command:
        task = command.split(' ')
        old_name = task[1]
        new_name = task[2]
        if old_name in my_list:
            wanted_index = my_list.index(old_name)
            my_list[wanted_index] = new_name

    elif 'Rearrange' in command:
        task = command.split(' ')
        item = task[1]
        if item in my_list:
            my_list.remove(item)
            my_list.append(item)

    command = input()

joined_list = ', '.join(my_list)
print(joined_list)
